/*
This file contains the icons that are used in the application when the FontAwesome license is in
use.  When additional icons are required, they should be imported in this file and added to the
`IconRegistry` array.
*/

/* let importedRegistry: ImportedRegistry = {
       regular: {} as ModuleImport<"regular">,
       solid: {} as ModuleImport<"solid">,
     };
     IconCodes.__ALL__.forEach(async (code: IconCode) => {
       const path = IMPORT_PATHS[licenseType][code];
       const m: ModuleImport<typeof code> = await import(path);
       IMPORT_ICONS[code].forEach((name: FontAwesomeIconName<typeof code>) => {
         importedRegistry = {
           ...importedRegistry,
           [code]: { ...importedRegistry[code], [name]: m[name] },
         };
       });
     }); */

/* import {
     faCircleQuestion,
     faFileImage,
     faFileVideo,
     faFileAudio,
     faFileArchive,
     faFilePdf,
     faFilePowerpoint,
     faFileExcel,
     faFileWord,
     faFile,
     faTrashAlt,
     faBookOpen as faBookOpenRegular,
     faFolderOpen as faRegularFolderOpen,
     faBook as faBookRegular,
   } from "@fortawesome/pro-regular-svg-icons";
   import {
     faArrowDown,
     faArrowUp,
     faDatabase,
     faTriangleExclamation,
     faCircleCheck,
     faCircleExclamation,
     faXmark,
     faUserCircle,
     faChevronDown,
     faFileCsv,
     faBank,
     faFileImport,
     faCircleNotch,
     faServer,
     faCaretDown,
     faCaretUp,
     faTimesCircle,
     faPlusCircle,
     faFilter,
     faSortAmountDown,
     faCameraAlt,
     faCopy,
     faUsers,
     faAddressBook,
     faFolderOpen as faSolidFolderOpen,
     faBookOpen as faBookOpenSolid,
     faBook as faBookSolid,
   } from "@fortawesome/pro-solid-svg-icons"; */

/* export const PRO_ICON_REGISTRY = [
     faArrowDown,
     faArrowUp,
     faDatabase,
     faTriangleExclamation,
     faCircleCheck,
     faCircleExclamation,
     faXmark,
     faUserCircle,
     faChevronDown,
     faCircleQuestion,
     faFileImage,
     faFileVideo,
     faFileAudio,
     faFileArchive,
     faFilePdf,
     faFilePowerpoint,
     faFileExcel,
     faFileWord,
     faFileCsv,
     faFile,
     faBank,
     faFileImport,
     faCircleNotch,
     faServer,
     faCaretDown,
     faCaretUp,
     faTimesCircle,
     faPlusCircle,
     faTrashAlt,
     faFilter,
     faSortAmountDown,
     faCameraAlt,
     faSolidFolderOpen,
     faRegularFolderOpen,
     faCopy,
     faUsers,
     faBookOpenSolid,
     faBookOpenRegular,
     faAddressBook,
     faBookSolid,
     faBookRegular,
   ] as const; */

export {};
